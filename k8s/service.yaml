apiVersion: v1
kind: Service
metadata:
  name: pocketlibrary-service
spec:
  type: NodePort
  selector:
    app: pocketlibrary-server
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080
